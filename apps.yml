receipt:
  - directory: 'receipt'
  - services:
      - bot: 'receipt-bot.service'
      - web: 'webhook-back.service'
webhook-server:
    - directory: 'webhook'
    - services:
        - web: 'webhook-back.service'
